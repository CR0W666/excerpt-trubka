import { ExcerptTrubkaPipe } from './excerpt-trubka.pipe';

describe('ExcerptTrubkaPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcerptTrubkaPipe();
    expect(pipe).toBeTruthy();
  });
});
